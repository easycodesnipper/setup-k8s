---
- name: Install Flannel network plugin
  ansible.builtin.command: kubectl apply -f {{ k8s_flannel_manifest_file }}
  become: false
  register: flannel_result
  until: flannel_result.rc == 0
  retries: 3
  delay: 10
  changed_when: flannel_result.rc == 0
