---
# roles/plugin/tasks/cni-calico.yml

- name: Install CNI Calico
  ansible.builtin.include_tasks: _install_plugin.yml
  vars:
    plugin_name: cni-calico
    plugin_config: "{{ k8s_plugins[plugin_name] }}"
  tags: plugin-calico
