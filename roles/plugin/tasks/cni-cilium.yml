---
# roles/plugin/tasks/cni-cilium.yml

- name: Install CNI Cilium
  ansible.builtin.include_tasks: _install_plugin.yml
  vars:
    plugin_name: cni-cilium
    plugin_config: "{{ k8s_plugins[plugin_name] }}"
  tags: plugin-cilium
