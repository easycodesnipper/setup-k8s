---
# roles/plugin/tasks/headlamp.yml

- name: Install Headlamp
  include_tasks: _install_plugin.yml
  vars:
    plugin_name: headlamp
    plugin_config: "{{ k8s_plugins[plugin_name] }}"
  tags: plugin-headlamp
