---
# roles/plugin/tasks/metallb.yml

- name: Install MetalLB
  include_tasks: _install_plugin.yml
  vars:
    plugin_name: metallb
    plugin_config: "{{ k8s_plugins[plugin_name] }}"
  tags: plugin-metallb
